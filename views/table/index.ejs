<% include ../partials/header %>





<div class="container">
	<div class="jumbotron"> 
		<h1>Mesa Directiva</h1>
		<h3>Aqu&iacute; podr&aacute;s encontrar los datos de la mesa directiva</h3>
		<% if(user.admin){ %>
		<div class="row">
		    <a href="/table/new" class="btn btn-success">Agregar nuevo</a>
		</div>
        <% } %>
	</div>
</div>

<div class="container">
	<div class="row">
        <% table.forEach(function(table){ %>
            <div class="col-md-6 col-sm-12">
        	    <div class="thumbnail">
        		    <div class="row">
        			    <div class="col-xs-4">
        			        <img src="<%= table.image %>" class="img-responsive img-rounded" alt="Responsive image" id="image">
                        </div>
        				<div class="col-xs-8">
        				    <h1><%= table.name %></h1>
        				    <h3><%= table.job %></h3>
        				    <% if(user.admin){ %>
        				    <div class="row">
        				        <div class="col-md-2 col-sm-12 buttrigth">
            				        <input type="button" class="btn btn-info"onclick="location.href='/table/<%=table._id%>/edit';" value="Editar" />
            				    </div>
            				    <div class="col-md-2 col-sm-12 butt">
            				        <form method="POST" action="/table/<%=table._id%>?_method=DELETE">
            				            <button class="btn btn-danger">Eliminar</button>
            				        </form>
            				    </div>    
            				    
        				    </div>
        				    <% } %>
        				    
        				</div>
        			</div>	
        		</div>
        	</div>
        <% }) %>
    </div>
</div>
<% include ../partials/footer %>
